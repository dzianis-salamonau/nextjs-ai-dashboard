# Auth (generate with: openssl rand -base64 32)
AUTH_SECRET=your-secret-here

# Optional: enables live OpenAI streaming. Without it, demo mode streams canned responses.
OPENAI_API_KEY=

# App URL (production)
NEXTAUTH_URL=http://localhost:3000
